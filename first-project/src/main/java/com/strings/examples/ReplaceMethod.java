package com.strings.examples;

public class ReplaceMethod {
    public static void main(String[] args) {
        //replace()
        //single or multiple characters it will replace in a String

        String str = "Selenium Java Selenium Python Selenium JS SELENIUM";
        System.out.println(str);
        System.out.println(str.replace("Selenium", "Playwright"));
    }
}
